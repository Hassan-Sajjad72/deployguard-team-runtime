apiVersion: 1

providers:
  - name: DeployGuard
    orgId: 1
    folder: DeployGuard
    type: file
    disableDeletion: true
    editable: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
